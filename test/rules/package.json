{
  "name": "taskcaster-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore + Storage security rules tests for TaskCaster, run against the Firebase emulators. See run.sh.",
  "scripts": {
    "test": "mocha --timeout 120000 ./*.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "mocha": "^10.7.3"
  }
}
