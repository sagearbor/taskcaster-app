<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build only (lives under src/debug, never merged into a release
  build). Lets the app reach the Firebase Local Emulator Suite
  (Firestore/Auth) running on the host machine over plain HTTP — the
  Android emulator sees the host as 10.0.2.2. targetSdk 28+ blocks
  cleartext traffic by default, so this narrow allowlist is needed for
  emulator-backed tests (see integration_test/delete_account_emulator_test.dart).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
